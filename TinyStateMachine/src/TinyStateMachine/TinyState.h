#pragma once
#ifndef TINY_STATE_H
#define TINY_STATE_H

#include "State.h"

typedef void (*CallbackFunction) ();
typedef bool (*GuardCondition) ();

class TinyState
{
private:
protected:
public:
  TinyState();
};

#endif TINY_STATE_H
